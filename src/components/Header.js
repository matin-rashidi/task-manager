import React from 'react';

const Header = () => {
    return (
        <header className='w-full py-2 bg-teal-800 text-white text-center font-semibold'>
            <h1>Task Manager</h1>
        </header>
    );
};

export default Header;